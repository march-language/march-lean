mod M do
  fn peer(n : Int) : Int do
    if n == 0 do 0 else spin(n + 1) + 1 end
  end
  fn spin(n : Int) : Int do
    fn peer(x : Int) : Int do x + 1 end
    peer(n)
  end
  fn main() do println(int_to_string(spin(1))) end
end
